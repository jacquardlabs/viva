# Pre-mortem — Show per-section revision depth in the card header

- Branch: epic/reviewer-experience--revision-depth
- SHA: d139021
- Date: 2026-08-02

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | The historical walk includes round `N` from disk *and* adds the in-hand `/next-round` payload, double-counting the current round — every multiplier reads one too high. The design's hybrid source (disk for 1..N-1, memory for N) is exactly what makes this reachable. | Fixture with `review-input-r1..r3.json` on disk, serve round 3 where r2 and r3 both carry a `diff` for one section; assert the card reads `2×`, not `3×`. |
| 2 | technical | A missing or unparseable historical round file makes the badge read `△ 06` with no multiplier — an assertion of "revised once", not an absence. Same plausible-looking-lie class the doc disqualified in-memory accumulation for (Alternatives item 1), in the deflating direction. | Delete `.viva/review-input-r2.json` mid-session, reload `/input`, and check what the card claims for a section revised in r2 and r4 — a bare triangle is the failure, not the pass. |
| 3 | technical | The N-1 file reads land inside the `with _data_lock:` block at `server.py:3690-3693`, so every `GET /input` holds the lock across disk I/O and stalls concurrent SSE pushes. | Read the `/input` and `/next-round` handlers: any `open()`/`read_text()`/`json.load` textually inside a `with _data_lock:` body is the defect. |
| 4 | technical | The multiplier is plain text inside `<button class="card-head">` (`server.py:538`) where only the △ glyph is `aria-hidden`, so the button's accessible name becomes "Error Handling 03 2×" — a screen-reader regression the design doc's "no new a11y treatment is needed" claim does not cover. | Inspect the emitted `.rev-tri` markup: the new span carries no `aria-hidden` and the head button gains no `aria-label`. |
| 5 | technical | `.rev-tri` grows from ~5 to ~8 characters while active-card `.card-title` has no ellipsis (only `.carried-head .card-title` does, `server.py:463`), so long titles wrap sooner and card heads grow taller — the row-crush shape `b4af98f` (#97) already fixed once for the Q&A choice badge. | Render a section whose title runs past one line at the narrow end of the viewport range, with and without the multiplier; compare card-head height and wrap point. |
| 6 | product | Badge saturation on a hard-fought review: by round 6 most active cards carry a multiplier, so the badge stops discriminating exactly when depth matters most and "which section is the problem child" is no longer answerable at a glance. | Dogfood a 6-round review on a 10+ section doc and count what fraction of active cards show a multiplier; above roughly half, the signal has flattened. |
| 7 | product | An agent that retitles a section during a rewrite breaks `schema.section_key` identity, so `_compute_diffs` finds no prior match, the `diff` never lands, and an accumulated count disappears — the card reads "never revised" for a thrice-revised section. | Rewrite a section with a changed heading between rounds; the triangle and multiplier both vanish rather than the count merely resetting. |

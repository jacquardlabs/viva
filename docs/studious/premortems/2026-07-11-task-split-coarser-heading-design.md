# Pre-mortem — Fix silent absorption of coarser-heading content in task-card split

- Design doc: docs/superpowers/specs/2026-07-11-task-split-coarser-heading-design.md
- Branch: epic/jig-integration--task-split-coarser-heading
- SHA: 100c129
- Date: 2026-07-11

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | The 7-line patch is threaded into shared post-branch code (or the `if split_on is not None:` block) instead of living strictly inside the auto-detect `else` branch, so `--split-on`'s deliberate "coarser heading stays absorbed" behavior regresses. | `git diff scripts/parse_sections.py` must show zero changed lines inside `if split_on is not None:`; `test_split_on_ignores_coarser_repeated_heading` and `test_split_on_matches_regardless_of_depth` must pass unmodified. |
| 2 | technical | The `idx > first_split_idx` guard is dropped, or written as `>=` / `<`, promoting the document's `# Title` and lead-in into their own one-line card and breaking preamble/h1 handling for effectively every doc reviewed today. | `test_preamble_uses_h1_title` / `test_preamble_empty_omitted` fail, or a plain doc suddenly parses one extra leading card before its first real section. |
| 3 | technical | The Revision-History side-effect (a coarser trailing `## Revision History` now enters `split_headings`, so `rev_line` resolves to it) interacts badly with the integrity check's exempt region — ledger chars double-counted, dropped, or the ledger rendered as a task card. | Parse a fixture with a trailing coarser `## Revision History`: `_integrity_check` must not raise, the ledger block must be excluded (not inside the last task's `content`), and the follow-up card must appear only if it is genuinely a non-ledger heading. |
| 4 | technical | The regression test asserts only the happy path (trailing coarser heading is promoted) and omits the guard's negative case, so a later refactor can silently drop `idx > first_split_idx` with the suite still green. | The new test file must include a case where a coarser heading sits *before* the first split-level heading and assert it stays folded into the preamble — not merely the promotion case and a `len(sections)` count. |
| 5 | product | A coarser heading interleaved mid-document (`### Task 1`, `## Aside`, `### Task 2`) fragments prose the author meant as one task's trailing content into a separate card, surprising the reviewer with an orphaned-sentence card rather than a real unit of trust. | Acceptance walkthrough of the interleaved fixture: confirm the promoted card's title and content are what a reviewer would recognize as a distinct section, not a stray sentence severed from Task 1. |
| 6 | product | For a doc already mid-round when the fix ships, the affected task's approval silently stops carrying forward (the section boundary moved, breaking byte-identity in `_load_approved`) and the reviewer sees a previously-approved task flip to pending with no in-UI explanation of why. | Reparse a prior-round `.viva/` input against the new boundary: verify the approval drop is caused only by the content-change path `_load_approved` already documents, and that the flip reads to the user as "content changed, re-review" rather than an unexplained reset. |

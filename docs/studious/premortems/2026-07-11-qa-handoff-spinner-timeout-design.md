# Pre-mortem — Client-side timeout affordance for the qa→review processing spinner

- Design doc: docs/superpowers/specs/2026-07-11-qa-handoff-spinner-timeout-design.md
- Branch: epic/jig-integration--qa-handoff-spinner-timeout
- SHA: 98e9d62
- Date: 2026-07-11

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Timer leaks past a hide path: the `setTimeout` is armed in the `processing` listener (server.py:2660) but cleared only in the `round` (2666) and `complete` (2697) listeners. Any other path that sets `processing-view` `display:none` — an added fourth transition, or a `complete` that `es.close()`s before the handle is cleared — leaves the timer running and fires "Still waiting" over an already-hidden view. | At merge, grep every `processing-view').style.display = 'none'` and confirm each clears the stored timeout handle; verify the handle is module-scoped and reset, not shadowed per-listener. |
| 2 | technical | Escalation inversion: the design requires `onerror` to remove a still-waiting banner and replace it with the connection-lost one, but `onerror`'s existing guard is `if (!el('sse-error-banner'))` (server.py:2726). If the soft banner reuses that same id, a real connection drop after the soft timer fired sees the id already present, early-returns, and suppresses the harder "Connection lost" signal — the softer message wins exactly when the harder one matters most. | Read `es.onerror` at merge: it must unconditionally remove any existing `#sse-error-banner` before creating the connection-lost banner, not early-return when one exists. Verify both banners' ids and the mutual-exclusion direction. |
| 3 | technical | Constant guessed, not measured: the doc defers the timeout value to build with a 15–30s range and explicitly asks for "an actual timing run of a realistic jig synthesis." Build ships a bare guessed constant with no timing basis, mistuned high (Journey B human waits too long) or low (Journey A false positives). | Check the commit for the named constant carrying a comment or PR note citing a measured submit→reflow gap; a bare number with no timing evidence is the tell. |
| 4 | technical | Test pins presence, not behavior: per the feasibility note the build asserts string-needles against `server.HTML` (the constant, the banner fn, the mutual-exclusion check). A timer that is present-but-broken — never cleared, fires early, banner never removed on late `round` — passes every string assertion. The timed/interactive behavior rides entirely on a manual browser check that can be skipped. | Confirm the manual browser verification named in Open questions actually ran — timed appearance, disappearance on a late `round`, and the soft→connection-lost escalation — rather than a green string-needle suite standing in for it. |
| 5 | product | False positive on a slow-but-healthy Journey A revise: a legitimately large in-session revise outlasts the threshold, so the human sees "Still waiting — check the terminal." during a normal round. If the constant is shorter than a real P90 revise, the human abandons a session about to succeed or learns to ignore the banner (crying-wolf), eroding the signal for Journey B where it matters. | Time the submit→`round` reflow gap on a large multi-section doc against the chosen constant; if the constant is below a realistic revise duration, the banner fires on healthy rounds. |
| 6 | product | "Check the terminal" points at an ambiguous process in Journey B: the hand-off's stuck process is the external caller (jig's `/design` synthesis), which may run in a different terminal than the viva agent — or in none the human has visible. The copy is borrowed verbatim from the connection-lost banner, whose "terminal" is the viva agent's; a human reading it during a hand-off stall may inspect the wrong process. | Read the shipped banner copy against Journey B — does anything disambiguate which process to inspect, or does it silently assume the viva-agent terminal is the one to check? |

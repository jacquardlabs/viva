# Pre-mortem — Unified Q&A → Review Session

- Design doc: docs/superpowers/specs/2026-07-11-unified-session-design.md
- Branch: epic/jig-integration--unified-session
- SHA: a80a501
- Date: 2026-07-11

| # | Lane | Failure mode | Detection hint |
|---|------|--------------|----------------|
| 1 | technical | Stuck processing beat: after the human submits Q&A, `/submit` fires the `processing` SSE event (server.py:3085) and the browser hides both qa/review views for an indeterminate spinner with no timeout. If jig's out-of-viva synthesis fails or never POSTs round-1, the human is stranded on the spinner — the doc's failure-mode section addresses only the terminal/agent side ("qa server left running"), not the human's tab. | Check for any browser-side timeout or failure affordance on `processing-view`; confirm the design's failure-mode was extended to cover the tab, not just the agent. |
| 2 | technical | The `round` SSE handler (server.py:2666-2680) shows `review-view` and hides `processing-view` but never hides `qa-view`; the happy path only works because `/submit`'s `processing` event hid qa-view first. A `/next-round` POST not preceded by a `/submit` layers review cards under a still-visible Q&A view — the exact gap the doc names, reachable whenever that event-ordering assumption breaks. | Grep the round SSE listener at merge for an explicit `el('qa-view').style.display='none'`; do not accept reliance on a prior `processing` event's ordering. |
| 3 | technical | A qa-originated review round is indistinguishable from any other round: `/next-round` (server.py:3110-3121) validates and rebroadcasts any `sections` payload identically to a review/diff round advance, with nothing marking it as "followed a Q&A phase." The epic pre-mortem item 5 and this doc's Open question #2 both flag it. | Inspect the round-1 payload the implementation POSTs — absent a distinguishing field/marker, #111's headless-contract must reverse-engineer this session type. Verify Open question #2 was resolved, not dropped. |
| 4 | technical | Shared `_output_path` footgun: `/next-round` sets `_output_path` from its body (server.py:3118). If the review round-1 payload's `output` is left pointing at the qa `--output` (`.viva/answers.json`), the first review `/submit` overwrites the answers. Attachment namespacing (`qa` vs `r{rnd}`, server.py:2962-2973) protects the attachments dir but not the output file itself. | Confirm the review round's `output` is a distinct path from the qa `--output`; add a regression assertion that answers.json survives the handoff. |
| 5 | product | Q&A context vanishes at the transition: the design promises "sections that came directly out of the answers they just gave," but the handoff replaces the Q&A cards with no on-screen trace. A reviewer who wants to recall which answer drove a given section has nothing to refer back to. | Acceptance walkthrough — after the reflow, is there any affordance tying review sections back to the answers, or is the Q&A context gone entirely? |
| 6 | product | Silent multi-second gap reads as a hang: jig's synthesis (drafting sections from answers) runs outside viva and may take many seconds while the human sees only an indeterminate processing-view. Against PRODUCT.md principle 5 (never make the human wait on a tool round-trip), a silent gap after the continuity promise risks the human thinking the session died and closing the tab. | Acceptance — time the submit→reflow gap in a realistic jig run; check whether processing-view communicates "turning your answers into sections" versus a bare spinner. |
